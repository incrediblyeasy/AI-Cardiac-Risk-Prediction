# EchoFuseNet (Paper 1) — core dependencies
# Python 3.11. Pins reflect the versions validated on Day 1.

# Deep learning
torch>=2.2

# Numerics / signal processing
numpy>=1.26
scipy>=1.11

# ECG record I/O (MIT-BIH, INCART, PTB-XL are WFDB-format)
wfdb>=4.1

# Signal-to-image transforms (RP / GAF / MTF) — used from Day 3 onward
pyts>=0.13

# Visualization (sample transform figures under docs/figures/)
matplotlib>=3.7

# Hyperparameter optimization (§3) — optional; only needed for tuning runs.
# Install with: pip install optuna>=3.4  (or: pip install -e '.[tuning]')
# optuna>=3.4

# ONNX deployment (§8) — optional; only needed for export/quantization runs.
# Install with: pip install onnx onnxruntime  (or: pip install -e '.[deployment]')
# onnx>=1.16
# onnxruntime>=1.17
# onnxscript>=0.1

# Testing
pytest>=8.0
